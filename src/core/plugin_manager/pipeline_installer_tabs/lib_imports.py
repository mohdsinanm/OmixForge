import matplotlib, numpy, pandas, Bio, bs4

# The above imports are necessary to ensure that the required libraries are available
# for the plugin management system. They may be used by various plugins that can be
# installed, so we import them here to avoid import errors when those plugins are loaded.
# Note: These imports are not directly used in this file, but are essential for plugin functionality.
# Create PR to support more libraries as needed.